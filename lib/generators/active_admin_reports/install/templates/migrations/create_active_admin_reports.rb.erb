class Create<%= report.name.pluralize %> < ActiveRecord::Migration[<%= ActiveRecord::Migration.current_version.to_s %>]
  def change
    create_table <%= ":#{report.plural}" %> do |t|
      t.string :name, default: ''
      t.text :description, default: ''
      t.text :ruby_script, default: ''

      t.timestamps
    end
    add_index <%= ":#{report.plural}" %>, %i[name]
  end
end
